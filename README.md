# 👗 Fashion Advisor - AI-Powered Wardrobe App

## 📋 Обзор на проекта

**Fashion Advisor** е мобилно приложение за управление на гардероб с интегриран AI агент. Фокусът е върху **максимизиране на използването на съществуващите дрехи** за пестене от нови покупки и по-устойчива мода.

---

## 📊 АНАЛИЗ НА КОНКУРЕНТИТЕ

### 🔵 Whering - Digital Wardrobe & Outfit Planner
**Статистики:** 9M+ потребители, 4.5★ (19K ревюта), iOS & Android

#### ✅ Какво харесват потребителите:
- Безплатно премахване на фон от снимки
- Огромна база данни с 100M+ дрехи за лесно добавяне
- Cost-Per-Wear (CPW) - показва реалната стойност на всяка дреха
- "Dress Me" - AI генератор на аутфити в стил Clueless
- Статистики за носене - какво носиш често/рядко
- Cloud синхронизация между устройства
- Chrome разширение за добавяне от уебсайтове
- Packing lists за пътувания
- Социални функции - споделяне с приятели

#### ❌ Проблеми от ревютата:
1. **Бъгове и crash-ове** - приложението често се срива при много елементи (80+)
2. **Бавно качване** - понякога отнема много време
3. **Неточно разпознаване** - AI често греши с цветове и категории
4. **Липсващи цветове** - няма достатъчно опции (напр. "army green")
5. **Липсващи материали** - няма опция "Unknown" за неизвестен материал
6. **Търсене на брандове** - трябва да scrollваш през целия списък
7. **Outfit suggestions включват дрехи, които НЕ притежаваш** - опит да те накарат да купуваш
8. **Glitches при изтриване** - показва дублирани артикули
9. **Outfit-ориентиран подход** - неудобен за тези, които mix-and-match
10. **Само снимка в picker** - трудно разграничаване на подобни дрехи

---

### 🟣 Fits - Outfit Planner & Closet
**Платформа:** iOS само

#### ✅ Какво харесват потребителите:
- **Най-добрият outfit maker** - можеш да resize всеки артикул на виртуално тяло
- Реалистично изглеждащи аутфити (не просто 3 реда един върху друг)
- **Без лимит за безплатни потребители** (за разлика от Acloset - 100 артикула лимит)
- Чист UI без social компоненти
- Няма натрапчиви реклами за покупки

#### ❌ Проблеми:
- Само iOS (без Android)
- По-малко опции за метаданни (материал и др.)

---

### 🟢 Acloset / Stylebook / Други
#### Общи проблеми от ревютата:
- **Платени абонаменти** - много приложения станаха subscription-based ($100+/год)
- **Заключени данни** - при прекратяване на абонамент губиш всичко
- **Без експорт на данни** - не можеш да изнесеш гардероба си
- **Слаб outfit генератор** - предлага един и същ артикул всеки ден
- **Ръчно премахване на фон** - отнема много време
- **Локално съхранение** - риск от загуба на данни

---

## 🎯 НАШИТЕ ДИФЕРЕНЦИАТОРИ

### 1. 🤖 **AI Стилист Агент - Чат & Глас**
- Истински функционален AI агент (не просто random shuffle)
- Комуникация чрез текстов чат И гласови команди
- Разбира контекст: време, повод, настроение, сезон
- Учи от твоите предпочитания с времето
- Предлага САМО от твоите дрехи - никога не рекламира покупки

### 2. 💰 **Философия "Носи повече, пазарувай по-малко"**
- Фокус върху максимално използване на съществуващия гардероб
- Показва "забравени" дрехи
- Gamification за носене на всички дрехи
- Статистики колко пари си спестил като НЕ си купувал ново

### 3. 📱 **Native Mobile-First Experience**
- Изцяло мобилно приложение (iOS & Android)
- Гладък интерфейс като Whering/Fits
- Бързо и отзивчиво

### 4. 🔓 **Твоите данни, твой контрол**
- Експорт на данни по всяко време
- Офлайн режим с локално съхранение
- Cloud backup (опционален)

---

## 🏗️ ТЕХНИЧЕСКИ ПЛАН

### Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    MOBILE APP (React Native)                 │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────────┐│
│  │  Wardrobe   │ │   Outfit    │ │    AI Stylist Chat      ││
│  │  Manager    │ │   Creator   │ │    (Text + Voice)       ││
│  └─────────────┘ └─────────────┘ └─────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      BACKEND (n8n + Supabase)               │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────────┐│
│  │   Auth &    │ │   Image     │ │      AI Agent           ││
│  │   User API  │ │  Processing │ │   (OpenAI/Claude)       ││
│  └─────────────┘ └─────────────┘ └─────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE (Supabase)                       │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────────┐│
│  │ PostgreSQL  │ │   Vector    │ │     Storage             ││
│  │  (Items,    │ │  Database   │ │     (Images)            ││
│  │   Outfits)  │ │ (AI Search) │ │                         ││
│  └─────────────┘ └─────────────┘ └─────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

### Технологии

| Компонент | Технология | Защо? |
|-----------|------------|-------|
| **Mobile App** | React Native + Expo | Cross-platform (iOS + Android), бързо развитие |
| **UI Framework** | React Native Paper / NativeWind | Гладък, модерен дизайн |
| **Backend/API** | Supabase + Edge Functions | Бърз, serverless, вградена автентикация |
| **Database** | Supabase PostgreSQL | Надежден, скалируем |
| **Vector DB** | Supabase pgvector | За AI семантично търсене на дрехи |
| **Image Storage** | Supabase Storage | Интегрирано с базата |
| **AI Agent** | OpenAI GPT-4 / Claude | Разбиране на естествен език |
| **Background Removal** | remove.bg API / rembg | Премахване на фон от снимки |
| **Voice Recognition** | React Native Voice | Гласови команди |
| **Orchestration** | n8n (self-hosted) | Workflow автоматизация за AI агента |

### Защо Supabase + pgvector за AI?

**Предимства:**
1. **Векторно търсене** - AI агентът може да търси семантично ("покажи ми нещо подобно на тази червена рокля")
2. **Бързо** - векторните заявки са оптимизирани
3. **Интегрирано** - всичко в една база, няма нужда от отделен vector DB
4. **Real-time** - Supabase поддържа real-time subscriptions

**Как работи:**
```
1. Потребителят качва снимка на дреха
2. AI анализира снимката и генерира embedding (вектор)
3. Векторът се съхранява в pgvector
4. Когато потребителят пита агента "дай ми нещо за парти",
   AI търси семантично подходящи дрехи чрез vector similarity
```

---

## 📱 ФУНКЦИИ НА ПРИЛОЖЕНИЕТО

### 🗂️ WARDROBE (Гардероб)

| Функция | Описание | Приоритет |
|---------|----------|-----------|
| Добавяне на дреха | Снимка + автоматично премахване на фон | P0 |
| AI категоризация | Автоматично разпознаване на тип, цвят, материал | P0 |
| Редактиране на детайли | Бранд, цена, дата на покупка, сезон, тагове | P0 |
| Персонализирани категории | Потребителят може да добавя свои категории | P1 |
| Търсене и филтриране | По всякакви критерии | P0 |
| Сортиране | По дата, цвят, честота на носене, CPW | P1 |
| Сезонни папки | Разделяне на летни/зимни дрехи | P1 |
| Премахнат лимит | Без ограничение на брой артикули | P0 |

### 👔 OUTFIT CREATOR (Създаване на аутфити)

| Функция | Описание | Приоритет |
|---------|----------|-----------|
| Drag & Drop Outfit Builder | Плъзгане на дрехи върху виртуална фигура | P0 |
| Resize на артикули | Преоразмеряване за реалистичен вид (като Fits) | P0 |
| Запазване като любим | С описание и тагове | P0 |
| Повод/Събитие тагване | Работа, парти, casual, спорт и др. | P0 |
| Календар за аутфити | Планиране какво да носиш | P1 |
| Shuffle/Randomize | AI предложения базирани на твоя стил | P1 |
| Lookbooks | Колекции от аутфити по тема | P2 |

### 🤖 AI STYLIST (AI Стилист)

| Функция | Описание | Приоритет |
|---------|----------|-----------|
| Текстов чат | Разговор с AI за стилови съвети | P0 |
| Гласови команди | "Какво да облека за интервю?" | P1 |
| Контекстуални предложения | Време, сезон, повод | P0 |
| Обучение от предпочитания | AI се учи от твоите избори | P1 |
| "Dress Me" функция | AI генерира пълен аутфит | P0 |
| Показване на забравени дрехи | "Не си носил това от 3 месеца" | P0 |
| Стилови съвети | Базирани на текущите ти дрехи | P1 |
| **НИКОГА не препоръчва покупки** | Фокус върху това, което имаш | P0 |

### 📊 ANALYTICS (Статистики)

| Функция | Описание | Приоритет |
|---------|----------|-----------|
| Cost-Per-Wear | Цена на носене за всяка дреха | P0 |
| Wear Rate | Честота на носене | P0 |
| Най-носени артикули | Top 10 | P0 |
| Забравени артикули | Какво не си носил скоро | P0 |
| Цветова палитра | Анализ на доминиращи цветове | P1 |
| Месечни/годишни отчети | Обзор на гардероба | P2 |
| Спестени пари | Калкулатор колко си спестил | P1 |

### 🧳 PACKING & TRAVEL

| Функция | Описание | Приоритет |
|---------|----------|-----------|
| Packing lists | Създаване на списък за пътуване | P1 |
| Автоматични предложения | Базирани на дестинация и продължителност | P2 |
| Споделяне на списък | С партньор/приятели | P2 |

### 💾 DATA & SYNC

| Функция | Описание | Приоритет |
|---------|----------|-----------|
| Cloud backup | Автоматично съхранение в облака | P0 |
| Офлайн режим | Работа без интернет | P1 |
| Експорт на данни | JSON/CSV за всичките ти дрехи | P0 |
| Импорт от други приложения | Миграция от Acloset, Whering и др. | P2 |

---

## 🗓️ ROADMAP

### Фаза 1: MVP (4-6 седмици)
- [ ] React Native проект setup с Expo
- [ ] Supabase setup (Auth, DB, Storage)
- [ ] Основен UI: Гардероб, Детайли на артикул
- [ ] Качване на снимки с премахване на фон
- [ ] AI категоризация (тип, цвят)
- [ ] Основен outfit creator
- [ ] Базова статистика (Cost-Per-Wear, Wear Rate)

### Фаза 2: AI Агент (2-3 седмици)
- [ ] Интеграция на OpenAI/Claude
- [ ] pgvector setup за семантично търсене
- [ ] Текстов чат интерфейс
- [ ] "Dress Me" AI функция
- [ ] Контекстуални препоръки (време, повод)

### Фаза 3: Voice & Polish (2-3 седмици)
- [ ] Гласови команди
- [ ] Подобрения на UI/UX
- [ ] Анимации и преходи
- [ ] Performance оптимизация
- [ ] Офлайн режим

### Фаза 4: Advanced Features (2-3 седмици)
- [ ] Календар за аутфити
- [ ] Packing lists
- [ ] Lookbooks
- [ ] Детайлни статистики
- [ ] Gamification елементи

### Фаза 5: Launch (1-2 седмици)
- [ ] Beta testing
- [ ] Bug fixes
- [ ] App Store / Play Store submission
- [ ] Marketing материали

---

## 📁 СТРУКТУРА НА ПРОЕКТА

```
fashion-advisor/
├── mobile/                     # React Native app
│   ├── app/                   # Expo Router screens
│   │   ├── (tabs)/           # Tab navigation
│   │   │   ├── wardrobe.tsx  # Гардероб
│   │   │   ├── outfits.tsx   # Аутфити
│   │   │   ├── stylist.tsx   # AI Стилист
│   │   │   └── stats.tsx     # Статистики
│   │   ├── item/[id].tsx     # Детайли на артикул
│   │   └── outfit/[id].tsx   # Детайли на аутфит
│   ├── components/           # Reusable components
│   ├── hooks/                # Custom hooks
│   ├── lib/                  # Utilities
│   │   ├── supabase.ts      # Supabase client
│   │   ├── ai.ts            # AI agent functions
│   │   └── storage.ts       # Image handling
│   ├── styles/              # Styles
│   └── types/               # TypeScript types
│
├── supabase/                  # Supabase configuration
│   ├── migrations/           # Database migrations
│   ├── functions/            # Edge functions
│   │   ├── process-image/   # Background removal
│   │   ├── categorize/      # AI categorization
│   │   └── stylist/         # AI agent endpoint
│   └── seed.sql             # Initial data
│
├── n8n/                       # n8n workflows (optional)
│   └── workflows/            # Exported workflows
│
└── docs/                      # Documentation
    ├── api.md               # API documentation
    └── design.md            # Design guidelines
```

---

## 🗄️ DATABASE SCHEMA

```sql
-- Users table (managed by Supabase Auth)
-- profiles extension
create table profiles (
  id uuid references auth.users primary key,
  username text,
  avatar_url text,
  created_at timestamp with time zone default now()
);

-- Clothing items
create table items (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id) on delete cascade,
  name text,
  category text not null, -- 'tops', 'bottoms', 'shoes', etc.
  subcategory text,       -- 't-shirt', 'jeans', 'sneakers', etc.
  color text[],           -- array of colors
  pattern text,           -- 'solid', 'striped', 'floral', etc.
  material text,
  brand text,
  size text,
  purchase_price decimal,
  purchase_date date,
  season text[],          -- 'spring', 'summer', 'fall', 'winter'
  occasions text[],       -- 'casual', 'work', 'formal', etc.
  tags text[],            -- custom user tags
  image_url text not null,
  image_no_bg_url text,   -- image with background removed
  is_active boolean default true,
  wear_count integer default 0,
  last_worn_at timestamp with time zone,
  notes text,
  created_at timestamp with time zone default now(),
  updated_at timestamp with time zone default now(),
  
  -- Vector embedding for AI similarity search
  embedding vector(1536)
);

-- Outfits
create table outfits (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id) on delete cascade,
  name text,
  description text,
  occasion text,
  season text[],
  tags text[],
  is_favorite boolean default false,
  wear_count integer default 0,
  last_worn_at timestamp with time zone,
  created_at timestamp with time zone default now(),
  updated_at timestamp with time zone default now()
);

-- Junction table for outfit items with positioning
create table outfit_items (
  id uuid primary key default gen_random_uuid(),
  outfit_id uuid references outfits(id) on delete cascade,
  item_id uuid references items(id) on delete cascade,
  position_x integer,     -- X position on canvas
  position_y integer,     -- Y position on canvas
  scale decimal default 1, -- Size multiplier
  z_index integer default 0, -- Layer order
  created_at timestamp with time zone default now()
);

-- Wear history for tracking
create table wear_history (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id) on delete cascade,
  item_id uuid references items(id) on delete cascade,
  outfit_id uuid references outfits(id),
  worn_at date not null,
  notes text,
  created_at timestamp with time zone default now()
);

-- Packing lists
create table packing_lists (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id) on delete cascade,
  name text not null,
  destination text,
  start_date date,
  end_date date,
  notes text,
  created_at timestamp with time zone default now()
);

create table packing_list_items (
  id uuid primary key default gen_random_uuid(),
  packing_list_id uuid references packing_lists(id) on delete cascade,
  item_id uuid references items(id) on delete cascade,
  is_packed boolean default false
);

-- AI conversation history
create table ai_conversations (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id) on delete cascade,
  message text not null,
  role text not null, -- 'user' or 'assistant'
  metadata jsonb,     -- context, suggestions, etc.
  created_at timestamp with time zone default now()
);

-- Indexes for performance
create index items_user_id_idx on items(user_id);
create index items_category_idx on items(category);
create index items_embedding_idx on items using ivfflat (embedding vector_cosine_ops);
create index outfits_user_id_idx on outfits(user_id);
create index wear_history_item_idx on wear_history(item_id);
create index wear_history_date_idx on wear_history(worn_at);

-- Enable Row Level Security
alter table items enable row level security;
alter table outfits enable row level security;
alter table outfit_items enable row level security;
alter table wear_history enable row level security;
alter table packing_lists enable row level security;
alter table packing_list_items enable row level security;
alter table ai_conversations enable row level security;

-- RLS Policies
create policy "Users can only see their own items"
  on items for all using (auth.uid() = user_id);

create policy "Users can only see their own outfits"
  on outfits for all using (auth.uid() = user_id);
```

---

## 🤖 AI AGENT DESIGN

### Промпт за AI Стилиста

```markdown
Ти си личен стилист AI асистент в приложение за управление на гардероб.

ВАЖНИ ПРАВИЛА:
1. НИКОГА не препоръчвай покупка на нови дрехи
2. Работиш САМО с дрехите, които потребителят има в гардероба си
3. Целта ти е да помогнеш на потребителя да носи ПОВЕЧЕ от съществуващите си дрехи
4. Показвай "забравени" артикули - неща, които не са носени скоро
5. Давай конкретни, практични съвети

КОНТЕКСТ:
- Текущо време: {weather}
- Сезон: {season}
- Гардероб на потребителя: {items_summary}
- Статистики за носене: {wear_stats}

При въпроси за аутфит:
1. Анализирай повода и контекста
2. Потърси подходящи комбинации от гардероба
3. Приоритизирай по-малко носени артикули
4. Обясни защо препоръчваш тази комбинация
```

### Workflow на AI Agent

```
1. Потребител задава въпрос (текст/глас)
2. Voice → Text (ако е глас)
3. AI анализира въпроса и извлича:
   - Повод (работа, среща, парти)
   - Стил (casual, formal)
   - Контекст (време, сезон)
4. Vector search в базата за релевантни дрехи
5. AI генерира препоръка
6. Отговорът се показва с визуализация на дрехите
```

---

## 🎨 UI/UX GUIDELINES

### Дизайн принципи (вдъхновени от Whering/Fits):
1. **Чист и минималистичен** - без претрупване
2. **Бързо добавяне** - max 3 tap-а за нов артикул
3. **Визуално-ориентиран** - големи снимки, малко текст
4. **Gesture-based** - swipe, drag-and-drop
5. **Тъмен и светъл режим**

### Цветова палитра:
- Primary: #8B5CF6 (Purple)
- Secondary: #F59E0B (Amber)
- Background Light: #F9FAFB
- Background Dark: #111827
- Text: #1F2937 / #F9FAFB

### Типография:
- Headers: Inter Bold
- Body: Inter Regular
- Accent: Inter Medium

---

## 🚀 СЛЕДВАЩИ СТЪПКИ

1. **Setup на проекта:**
   ```bash
   npx create-expo-app fashion-advisor --template tabs
   cd fashion-advisor
   npx expo install @supabase/supabase-js
   ```

2. **Supabase setup:**
   - Създаване на проект в supabase.com
   - Активиране на pgvector extension
   - Създаване на таблиците
   - Setup на Storage bucket за снимки

3. **Първи компоненти:**
   - Auth screens (Login/Register)
   - Wardrobe grid view
   - Add item flow

---

## 📝 БЕЛЕЖКИ

- n8n URL: https://n8n.simeontsvetanovn8nworkflows.site (съществуващ)
- GitHub: Uruchev/wardrobe-manager (съществуващ repo)

---

*Документ създаден: 11 Декември 2025*
*Версия: 1.0*
